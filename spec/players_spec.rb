require 'players'

describe Players do
  let(:players) {Players.new}

  it "allows player 1 to fill in empty space" do
  end
end

#Want to get user to input a token into a space. Need to specify what space and what token.
#Possibly use the 'gets' method to get the user to input data?